ymmsl_version: v0.2

# torax_nice_rd_controller -- Forward co-simulation, resistive-diffusion NICE peer, with the PCS controller.

imports:
- from lib.evolutive_rd import implementation evolutive_rd

models:
  torax_nice_rd_controller:
    description: Forward co-simulation, resistive-diffusion NICE peer, with the PCS controller.
    components:
      run:
        description: the co-simulation
        implementation: evolutive_rd
        ports: {}
