# Sourced by run_workflow.sh before every workflow run. `module load PDS`
# (setup_files/PDS.lua) is now expected to already be done before this runs --
# it sets up IMAS-Python/IDStools/NICE/IMAS-MUSCLE3/Waveform-Editor/
# TORAX-MUSCLE3/etc., which workflows/lib/local_programs.ymmsl depends on via
# $EBROOT* vars. Do NOT `module purge` here: that would wipe those out from
# under the actors this workflow is about to launch.

# ENV VARIABLES
ulimit -s unlimited
unset MPLBACKEND
export IMAS_VERSION="4.0.0"
